-- Drop the Adoption table
DROP TABLE IF EXISTS "Adoption";

-- Drop the Pet table
DROP TABLE IF EXISTS "Pet";

-- Drop the User table
DROP TABLE IF EXISTS "User";

-- Drop the Shelter table
DROP TABLE IF EXISTS "Shelter";

-- Drop the adoption_status ENUM type
DROP TYPE IF EXISTS adoption_status;

-- Drop the gender ENUM type
DROP TYPE IF EXISTS gender;

-- Drop the size ENUM type
DROP TYPE IF EXISTS size;

-- Drop the role ENUM type
DROP TYPE IF EXISTS user_role;